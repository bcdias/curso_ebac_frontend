function multiplicaNumeros(primeiroNumero: number, segundoNumero: number): number {
    const produto = primeiroNumero * segundoNumero;
    return produto
} 

function dizOla(nome: string): string {
    return `Olá ${nome}`
}

console.log(multiplicaNumeros(2,110));

console.log(dizOla('Mariana'))
